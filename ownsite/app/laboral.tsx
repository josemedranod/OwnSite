import { getImage, getText } from "@/lib/api";
import { Button, Flex, Image, Text } from "@chakra-ui/react";

export default async function Laboral() {
    return(
        <Flex>

        </Flex>
    );
}